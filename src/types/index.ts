export interface CountryCode {
  code: string;
  dialCode: string;
  flag: string;
  name: string;
}

export interface RecipientDetails {
  email: string;
  phoneNumber: string;
  countryCode: string;
}

